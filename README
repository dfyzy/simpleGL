Using OpenGL, GLFW, GLEW and libpng

You can draw stuff now.

TODO:
-Fonts
-Better texture loading(clusters)
-Buffer data proper resizing
-Callbacks
-Window resizability
-Stencil masks?
-Particles(if there is a need for further optimization)
-Embded shaders
-Audio
-Networking
