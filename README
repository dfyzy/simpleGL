Using OpenGL, GLFW, GLEW and libpng.

You can draw stuff now.

TODO:
-Fonts
-Better texture loading(clusters)
-Window resizability
-Particles????(because really, we already draw instances, surely there is no need for further optimization)
-Audio
-Custom shaders
-Networking
