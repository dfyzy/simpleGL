Using OpenGL, GLFW, GLEW and libpng(also boost/thread because my compiler has no thread)

You can draw stuff now.

TODO:
-Fonts
-Better texture loading(clusters)
-Buffer data proper resizing
-Callbacks
-Window resizability
-Particles(if there is a need for further optimization)
-Audio
-Networking
